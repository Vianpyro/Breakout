name: Normalize Repository Name

on:
  workflow_dispatch:
  workflow_call: {}
  push:
    branches:
      - '**'

permissions:
  contents: read

jobs:
  build:
    name: Normalize Repository Name
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Normalize repository owner and name
        id: repository_normalized
        run: |
          echo "name=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')"
          echo "name=$(echo '${{ github.repository }}' | tr '[:upper:]' '[:lower:]')" >> "$GITHUB_OUTPUT"

      - name: Extract repository owner and name
        id: repo_name
        run: |
          echo "owner=$(echo '${{ steps.repository_normalized.outputs.name }}' | cut -d'/' -f1)"
          echo "name=$(echo '${{ steps.repository_normalized.outputs.name }}' | cut -d'/' -f2)"
          echo "owner=$(echo '${{ steps.repository_normalized.outputs.name }}' | cut -d'/' -f1)" >> "$GITHUB_OUTPUT"
          echo "name=$(echo '${{ steps.repository_normalized.outputs.name }}' | cut -d'/' -f2)" >> "$GITHUB_OUTPUT"
